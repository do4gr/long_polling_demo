datasource db {
  url = "***"
  // provider = env("PROVIDER")
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
    binaryTargets = ["darwin"]
}

model Simple {
  id              Int        @id
  name     String?
  does_not_exist String?
}


//relations

//json

//arrays

//native types